# Only primaryColor is set: leaving the base palette unset keeps the app
# adaptive, so it follows each visitor's light/dark preference instead of
# forcing one look.
[theme]
primaryColor = "#7C3AED"

[browser]
gatherUsageStats = false
